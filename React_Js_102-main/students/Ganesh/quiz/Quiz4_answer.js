function rectangle(w, h)
{
    return w * h
}
const rectangle = function(w , h)
{
    return w * h
}
const  rectangle = (w ,h ) =>
{
    return w * h
}
const rectangle =(w ,h) => w * h



/* in this programm we will get error brcuase
we are using const so we cannot ressign again
instead of const if we used 'var' then we can assign values knowingly or unkonwingly for 'rectangle'*
but if we used const we can not do that
with the help of const function we can prevent function overriding also*/